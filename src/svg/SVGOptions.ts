export default class SVGOptions {
    // Dimensions of one heatmap value square (height and width are equal) (in pixels)
    squareDimension: number = 20;
    // Padding between the different squares of the heatmap (in pixels)
    squarePadding: number = 2;
    // Default font size of the visualization (in pixels)
    fontSize: number = 14;
    // Spacing between the visualization and the start of the row / column labels text (in pixels)
    visualizationTextPadding: number = 4;
}
